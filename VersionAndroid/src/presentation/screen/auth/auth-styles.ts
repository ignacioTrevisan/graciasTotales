import { StyleSheet } from "react-native";

export const stylesSesion = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
    },
    childContainer: {
        backgroundColor: '#333',
        borderRadius: 15,
    }
})